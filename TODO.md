
- [ ] Create a input function `input <register> <mode>`. *Takes a value from the user and store it a register, mode 0 store as a int or string test, mode 1 store as a string*

***Note:*** if there every a need for two more *OPCODES* to benefit from a variable argument system, i will implement it 
